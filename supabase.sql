create table if not exists public.surprises(id bigint generated by default as identity primary key,slug text unique not null,payload jsonb not null,payment_id text,order_id text,created_at timestamptz default now());alter table public.surprises enable row level security;